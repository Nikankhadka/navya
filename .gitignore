# ── Dependencies ──
node_modules/

# ── Build outputs ──
/dist
dist/
web-build/
android/app/build/
ios/build/
*.tsbuildinfo
*.js.map

# ── Expo generated ──
.expo/
expo-env.d.ts

# ── Environment & secrets ──
.env
.env.local
.env.*.local
!.env.example

# ── Supabase ──
# Local Supabase Docker volumes (if any)
# supabase/.branches/
# supabase/.temp/

# ── Testing & coverage ──
/coverage
/coverage-final.json
/playwright-report
/test-results
/blob-report

# ── Secret scanning ──
.gitleaks.toml
gitleaks-report.json

# ── Agent local overrides ──
.agent/local.md
.opencode/
.agent/

# ── OS files ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo
*~

# ── IDE / Editor ──
.vscode/
.idea/
*.code-workspace
*.sublime-project
*.sublime-workspace

# ── Debug / Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# ── Temporary ──
*.tmp
*.temp
.cache/
