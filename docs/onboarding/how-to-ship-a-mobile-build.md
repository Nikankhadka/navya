# How To Ship A Mobile Build

1. Ensure `npm run verify` passes
2. Confirm env values are correct
3. Confirm `eas.json` profiles are correct
4. Run internal build commands
5. Update `docs/execution/current-status.md` with build status and blockers
