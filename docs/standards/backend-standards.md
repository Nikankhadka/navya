# Backend Standards

- Supabase is the backend platform
- every schema change needs migration and RLS
- generate client types after schema changes
- backend-only AI access
- use feature flags for costly or risky features
